// Importa la función createContext de React. Esto es necesario para crear el contexto.
import { createContext } from "react";

// Crea un nuevo contexto llamado UserContext utilizando la función createContext.
// Este contexto servirá para compartir información del usuario o estados relacionados con el usuario en la aplicación.
export const CartContext = createContext();